#include <stdio.h>
#include <stdlib.h>

extern int fibonacci(int n);

int translateFib(char fibNum) {
	return 1;
}

int main(void) {

	int i=1;
	char fibNum;
	for(i=1; i <=20; i++){
		fibNum = fibonacci(i);
		debug_printf("fib of %d = %c\n", i, fibNum);
		translateFib(fibNum);
	}
}
