#include <stdio.h>
#include <stdlib.h>

extern int fibonacci(int n);

int translateFib(int fibNum) {
	return 1;
}

int main(void) {

	int i=1;
	int fibNum;
	for(i=1; i <=20; i++){
		fibNum = fibonacci(i);
		translateFib(fibNum);
	}
}
